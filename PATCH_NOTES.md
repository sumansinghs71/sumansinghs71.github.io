# Production Problems expansion

Adds 10 generalized production-AI failure patterns. The examples are intentionally synthetic/generalized: no employer names, incident IDs, hostnames, internal tables, employee names, ticket numbers, secret paths, exact incident timelines, or proprietary identifiers are included.

Replace `problems/index.html` with the included version and copy the new files under `problems/`.
